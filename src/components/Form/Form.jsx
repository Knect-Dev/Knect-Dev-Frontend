import { IonModal, IonContent, IonButton } from '@ionic/react';

import './Form.scss';

const Form = () => {
  return (
    <IonModal
      isOpen={true}
      swipeToClose={ }
    >

      <IonContent>Word</IonContent>
    </IonModal>
  )
}

export default Form;